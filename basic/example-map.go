package main

import (
	"fmt"
)

/*
Some pointers:
- Different types of keys and values in the same map is not allowed.
- Although key and value map can be of different types.
- Key types can be int, float, or any other built-in type.
*/

func map_usage(input_arr){
	for k,v := range input_arr{

	}
}

func main(){
 //
 var map_a  map[int]string = {}
	 90: "Dog",
	 91: "Cat",
	 92: "Cow",
	 93: "Bird",
	 94: "Rabbit"
 }
 fmt.Println(map_a)
}